config {
    type: "assertion",
    schema: "wikipedia",
    name: "assert_no_zero_views",
    description: "Verifies that total_views in daily_pageviews_by_title are never zero."
}

SELECT
  title,
  day,
  total_views
FROM
  ${ref("daily_pageviews_24")}
WHERE
  total_views = 0
